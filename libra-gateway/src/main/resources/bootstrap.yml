spring:
  application:
    name: libra-gateway
  profiles:
    active: @spring.active@
  cloud:
    config:
      discovery:
        service-id: libra-config
        enabled: true
      profile: @spring.active@
      label: master